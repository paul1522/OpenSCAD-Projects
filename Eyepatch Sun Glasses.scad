use <lib_eye_patch.scad>

$fn = 180;

r1 = 120 ; // radius
z1 = 45 ; // lens height
y1 = 59 ; // lens width at the top
y2 = 46 ; // lens width at the bottom
t1 = 1.2 ; // thickness
offset = 0 ;
h=7.00;

eye_patch(r1, z1, y1, y2, t1, offset, h);
